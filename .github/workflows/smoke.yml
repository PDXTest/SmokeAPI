name: Automated API tests using Postman CLI

on: push

jobs:
  automated-api-tests:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Postman CLI
        run: |
          powershell.exe -NoProfile -InputFormat None -ExecutionPolicy AllSigned -Command "[System.Net.ServicePointManager]::SecurityProtocol = 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://dl-cli.pstmn.io/install/win64.ps1'))"
      - name: Login to Postman CLI
        run: postman login --with-api-key ${{ secrets.PMAK-6596c1bbd3e0480f94441d9e-5ef6e38315ec59778ef0a3076141817047 }}
      - name: Run API tests
        run: |
          postman collection run "27898693-48a0b607-b3fd-4d8c-9baf-dba49184af85" -e "27898693-60e6ad67-1d4f-4a1e-9065-ed00d1bf91da"
